import { JbvErrorClassDirective } from './jbv-error-class.directive';

describe('JbvErrorClassDirective', () => {
  it('should create an instance', () => {
    const directive = new JbvErrorClassDirective();
    expect(directive).toBeTruthy();
  });
});
