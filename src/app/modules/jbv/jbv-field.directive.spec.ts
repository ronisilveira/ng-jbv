import { JbvFieldDirective } from './jbv-field.directive';

describe('JbvFieldDirective', () => {
  it('should create an instance', () => {
    const directive = new JbvFieldDirective();
    expect(directive).toBeTruthy();
  });
});
