import { JbvErrorMessageDirective } from './jbv-error-message.directive';

describe('JbvErrorMessageDirective', () => {
  it('should create an instance', () => {
    const directive = new JbvErrorMessageDirective();
    expect(directive).toBeTruthy();
  });
});
